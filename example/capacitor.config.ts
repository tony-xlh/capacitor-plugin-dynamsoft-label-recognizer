import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dynamsoft.textscanner',
  appName: 'text-scanner',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
